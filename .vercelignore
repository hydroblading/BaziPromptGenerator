# Dependencies (already ignored by default, but explicit for clarity)
node_modules

# Build artifacts and cache
.next/cache
dist/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store

# Environment files (sensitive data)
.env.local
.env*.local

# Legacy server files (not needed for Vercel - Next.js handles this)
index.js
server.js

# Nested project directory (if not needed for deployment)
bazi-mcp/

